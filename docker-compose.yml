version: "3"

services:
  #MongoDB
  mongo_db:
    container_name: database_container
    image: mongo:latest
    restart: always
    volumes:
      - mongo_db:/data/db

  app:
    build: .
    ports:
      - 8000:8000
    environment:
      APP_NAME: Ecommerce
      PORT: 8000
      MONGO_URL: mongodb+srv://cryptonfuturemedia1989_db_user:gPtmqIFk8JB2SVeI@cluster0.ejp9pmh.mongodb.net/
    depends_on:
      - mongo_db

volumes:
  mongo_db: {}